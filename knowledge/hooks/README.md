# Hook sources

Hook runtime data is currently stored in `src/data/hooks.ts`. New or corrected hook entries should record an InstantCMS source location, version, verification date, and confidence before being copied into runtime data.
