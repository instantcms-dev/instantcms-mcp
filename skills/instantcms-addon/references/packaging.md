# Packaging checks

An installation archive contains package metadata and SQL at its root plus the `package/` tree. Validate paths and syntax before creating an archive. Prefer the installed `instantcms-packager` skill for actual release packaging and incremental update archives.
